#include "prototipos/acciones.h"
#include "prototipos/materiales.h"
#include "prototipos/menu.h"
#include <stdio.h>

int main() {
  inicializarMenu();

/*
  Pieza peon = {NULL, 'P', 1, 0, 6, 0};
  //Tablero vacio, solo con el peon
  Tablero tablero;
  for (int i = 0; i < 8; i++) {
    for (int j = 0; j < 8; j++) {
      tablero.casillas[j][i] = NULL;
    }
  }
  tablero.casillas[0][6] = &peon;
  imprimirTablero(&tablero);

  //Array de movimientos posibles
  Move *movimientos = obtenerMovimientosArray(&tablero, &peon);
  for (int i = 0; i < numMovimientos; i++) {
    printf("x: %d, y: %d\n", movimientos[i].x, movimientos[i].y);
  }
  moverPieza(&tablero, &peon, 0, 1);
  imprimirTablero(&tablero);
  
*/

  return 0;
}

